# ServiceAccount for Kafkaesque brokers
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kafkaesque
  namespace: kafkaesque
  labels:
    app.kubernetes.io/name: kafkaesque
    app.kubernetes.io/component: broker
# Disable auto-mounting of service account token for security
automountServiceAccountToken: false
